Focuser API
===========

## Client Side 

We will expose the following APIs to the client for calling:

+ connect
+ disconnect
+ reconnect
+ scanning
+ polling
+ get_parameter
+ set_parameter

+ move_to
+ move_step
+ abort_move
+ get_movement_status
+ get_temperature
+ get_current_position
