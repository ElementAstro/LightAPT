Filterwheel API
===============

## Client Side 

We will expose the following APIs to the client for calling:

+ connect
+ disconnect
+ reconnect
+ scanning
+ polling
+ get_parameter
+ set_parameter

+ slew_to
+ get_filter_list
+ get_current_filter